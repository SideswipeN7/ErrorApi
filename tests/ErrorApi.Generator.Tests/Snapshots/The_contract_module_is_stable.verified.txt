// Generated by ErrorApi. Do not edit; regenerate from the API instead.

/** Every error code this API can return. */
export const API_ERROR_CODES = [
  "Orders.AlreadyPaid",
  "Orders.NotFound",
] as const;

export type ApiErrorCode = (typeof API_ERROR_CODES)[number];

/** RFC 9457 problem document as returned by this API. */
export interface ApiProblem<TCode extends ApiErrorCode = ApiErrorCode> {
  /** Stable machine-readable code. Switch on this, never on `title`. */
  code: TCode;
  status: number;
  title?: string;
  detail?: string;
  type?: string;
  instance?: string;
  /** Secondary failures accompanying the primary one, when the server attaches them. */
  errors?: { code: ApiErrorCode; detail?: string | null }[];
}

/** Narrows an unknown response body to a problem document. */
export function isApiProblem(value: unknown): value is ApiProblem {
  return (
    typeof value === "object" &&
    value !== null &&
    typeof (value as ApiProblem).code === "string" &&
    typeof (value as ApiProblem).status === "number"
  );
}

/** HTTP status each code is returned with. */
export const API_ERROR_STATUS: Record<ApiErrorCode, number> = {
  "Orders.AlreadyPaid": 409, // Order already paid
  "Orders.NotFound": 404, // No order exists for that id.
};

/** Failures of `GET /health`. */
export type GetHealthError = never;

/** Failures of `GET /orders/{id}`. */
export type GetOrdersByIdError =
  | ApiProblem<"Orders.NotFound">;

/** Failures of `POST /orders/{id}/pay`. */
export type PostOrdersByIdPayError =
  | ApiProblem<"Orders.AlreadyPaid">
  | ApiProblem<"Orders.NotFound">;

/** Codes reachable per endpoint, keyed by `"METHOD /route"`. */
export const API_ENDPOINT_ERRORS = {
  "GET /health": [],
  "GET /orders/{id}": ["Orders.NotFound"],
  "POST /orders/{id}/pay": ["Orders.AlreadyPaid", "Orders.NotFound"],
} as const satisfies Record<string, readonly ApiErrorCode[]>;
